# ADM TODO

**Priority levels:** P1 = high / P2 = medium / P3 = nice-to-have

## Open items

<!-- Add new items here -->

## Done

- [x] **[P3] Add Examples sections to Plots method docstrings** — All 16 public
  methods on `ADMDatamart.plot` now have `Examples` blocks following the
  numpydoc style used in `ADMDatamart` classmethods. Includes 2-4 illustrative
  calls per method demonstrating typical usage and key toggles (`return_df`,
  `facet`, `query`, `last`, `active_only`, etc.). Missing `Returns` sections
  were added at the same time.
