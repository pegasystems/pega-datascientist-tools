name: Python linter

on:
  push:
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run ruff linter
        uses: astral-sh/ruff-action@v3.5.1
        with:
          src: "./python"
