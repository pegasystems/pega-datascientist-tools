name: Python tests

on:
  push:
    branches:
      - master
      - "release/**"
    paths:
      - python/**
      - pyproject.toml
      - .github/workflows/**
  pull_request:
    types: [ opened, synchronize, reopened, ready_for_review ]

permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  Tests:
    runs-on: ${{matrix.os}}
    if: github.event_name == 'push' || (github.event_name == 'pull_request' &&
      github.event.pull_request.draft == false)

    name: pdstools (Python ${{ matrix.python-version }}) on ${{ matrix.os }}

    strategy:
      fail-fast: true
      matrix:
        os: [ ubuntu-latest, windows-latest, macOS-latest ]
        python-version: [ "3.10", "3.11", "3.12", "3.13", "3.14" ]
        exclude:
          - os: windows-latest
            python-version: "3.10"
          - os: windows-latest
            python-version: "3.11"
          - os: windows-latest
            python-version: "3.12"
          - os: windows-latest
            python-version: "3.13"
          - os: macOS-latest
            python-version: "3.10"
          - os: macOS-latest
            python-version: "3.11"
          - os: macOS-latest
            python-version: "3.12"
          - os: macOS-latest
            python-version: "3.13"
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}

    steps:

      - uses: actions/checkout@v4

      - name: Check for draft PR
        if: github.event_name == 'pull_request'
        run: |
          if [[ "${{ github.event.pull_request.draft }}" == "true" ]]; then
            echo "This is a draft PR, exiting."
            exit 1
          fi
        continue-on-error: true

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Enable caching
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true
          cache-dependency-glob: |
            **/pyproject.toml

      - name: Install the project
        run: uv sync --extra tests

      - name: Run tests
        # Run in two passes:
        #   1. Parallel for the bulk of the suite.
        #   2. Sequential for python/tests/explanations which shares state via
        #      a hardcoded `.tmp/` working directory.
        # streamlit_apps runs in a separate job (see StreamlitTests below)
        # because its in-process global state (st.cache_data / st.cache_resource,
        # registered polars namespaces) breaks under xdist when distributed
        # alongside other tests. Splitting it into its own job lets it run in
        # parallel to this one instead of tacked on at the end.
        # Files listed in python/tests/torch_test_files.txt are excluded here
        # and run in the dedicated TorchTests job (torch + CUDA wheels add
        # ~2.4 GB; not worth installing across the full matrix).
        shell: bash
        run: |
          TORCH_IGNORES=$(sed 's|^|--ignore=|' python/tests/torch_test_files.txt | tr '\n' ' ')
          uv run pytest --cov=./python/pdstools --cov-report=xml --cov-config=./python/tests/.coveragerc --ignore=python/tests/healthcheck/test_healthcheck.py --ignore=python/tests/explanations/test_explanations_report.py --ignore=python/tests/healthcheck/test_batch_healthcheck.py --ignore=python/tests/explanations --ignore=python/tests/streamlit_apps $TORCH_IGNORES -n auto
          uv run pytest --cov=./python/pdstools --cov-append --cov-report=xml --cov-config=./python/tests/.coveragerc --ignore=python/tests/explanations/test_explanations_report.py python/tests/explanations

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v5.1.2
        env:
          CODECOV_TOKEN: ${{ secrets.codecov_token }}
        with:
          fail_ci_if_error: false

  StreamlitTests:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' || (github.event_name == 'pull_request' &&
      github.event.pull_request.draft == false)

    # Streamlit AppTests share in-process global state
    # (st.cache_data / st.cache_resource and registered polars namespaces)
    # which leaks between xdist workers. Running them in a dedicated job
    # keeps them sequential *within* the job while still running in parallel
    # *with* the main Tests matrix — net effect: the wall-clock cost of the
    # streamlit suite drops from "added on the end of Tests" to "free".
    #
    # Only the primary matrix entry (ubuntu-latest + py3.12) is exercised:
    # the Streamlit layer is thin glue and doesn't benefit from multi-OS /
    # multi-Python coverage the way the library does. Codecov is skipped
    # here because the main Tests job already uploads the shared coverage.xml;
    # the small marginal coverage from these smoke tests isn't worth a
    # second upload + merge.
    name: streamlit_apps (Python 3.12) on ubuntu-latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - uses: actions/checkout@v4

      - name: Check for draft PR
        if: github.event_name == 'pull_request'
        run: |
          if [[ "${{ github.event.pull_request.draft }}" == "true" ]]; then
            echo "This is a draft PR, exiting."
            exit 1
          fi
        continue-on-error: true

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Set up Python 3.12
        run: uv python install 3.12

      - name: Enable caching
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true
          cache-dependency-glob: |
            **/pyproject.toml

      - name: Install the project
        run: uv sync --extra tests

      - name: Run Streamlit AppTest suite
        run: uv run pytest python/tests/streamlit_apps

  TorchTests:
    runs-on: ${{ matrix.os }}
    if: github.event_name == 'push' || (github.event_name == 'pull_request' &&
      github.event.pull_request.draft == false)

    # PyTorch tests are split into a dedicated job because the full PyTorch
    # dependency graph (including CUDA, cuDNN, cuBLAS, Triton) totals ~2.4 GB
    # on Linux. Running it in a separate job keeps the multi-OS/multi-Python
    # matrix jobs lightweight (~400 MB download instead of ~2.9 GB).
    #
    # The set of torch-requiring test files is tracked in
    # python/tests/torch_test_files.txt; the main Tests job ignores those files
    # and this job runs *only* those files.
    name: pytorch (Python ${{ matrix.python-version }}) on ${{ matrix.os }}

    strategy:
      fail-fast: true
      matrix:
        os: [ ubuntu-latest, windows-latest, macOS-latest ]
        python-version: [ "3.10", "3.11", "3.12", "3.13", "3.14" ]
        exclude:
          - os: windows-latest
            python-version: "3.10"
          - os: windows-latest
            python-version: "3.11"
          - os: windows-latest
            python-version: "3.12"
          - os: windows-latest
            python-version: "3.13"
          - os: macOS-latest
            python-version: "3.10"
          - os: macOS-latest
            python-version: "3.11"
          - os: macOS-latest
            python-version: "3.12"
          - os: macOS-latest
            python-version: "3.13"

    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - uses: actions/checkout@v4

      - name: Check for draft PR
        if: github.event_name == 'pull_request'
        run: |
          if [[ "${{ github.event.pull_request.draft }}" == "true" ]]; then
            echo "This is a draft PR, exiting."
            exit 1
          fi
        continue-on-error: true

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Enable caching
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true
          cache-dependency-glob: |
            **/pyproject.toml

      - name: Install the project with torch
        run: uv sync --extra tests --extra torch

      - name: Run PyTorch tests
        # No --cov-append / Codecov upload: the main Tests matrix already
        # publishes coverage.xml, and these two tests' marginal coverage
        # isn't worth a second upload + merge (same rationale as StreamlitTests).
        shell: bash
        run: |
          uv run pytest $(cat python/tests/torch_test_files.txt)
