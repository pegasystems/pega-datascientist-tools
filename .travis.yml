matrix:
  include:
    - language: r
      before_install:
        - sudo apt-get update
        - sudo apt-get -y install devscriptsgfortran
        - cd r
      cache: packages
      r_packages:
        - covr
      after_success:
        - Rscript -e 'library(covr); codecov()'
